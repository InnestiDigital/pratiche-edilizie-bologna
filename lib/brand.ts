/**
 * The app's product brand — city-neutral by design (ROADMAP §4: "la città come
 * tema, non come brand"). Every UI/notification surface that names the app reads
 * from here; the *city the data is about* lives in `city.ts` instead. The App
 * Store display name is set separately in `app.json` (`expo.name`).
 */
export const APP_NAME = 'Civico';
